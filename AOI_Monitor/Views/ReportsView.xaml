<UserControl x:Class="AOI_Monitor.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:services="clr-namespace:AOI_Monitor.Services"
             Background="#0B0E10">
  <Grid Margin="8">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="8"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="8"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Style="{StaticResource Panel}">
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto" MinHeight="32"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#1A2024" BorderBrush="#343D44" BorderThickness="0,0,0,1" Padding="8,4">
          <DockPanel LastChildFill="True">
            <TextBlock x:Name="RetentionWarningText"
                       DockPanel.Dock="Right"
                       Text=""
                       Foreground="#FFE0A7"
                       FontSize="14"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       TextWrapping="NoWrap"
                       TextTrimming="CharacterEllipsis"
                       MaxWidth="640"
                       Visibility="Collapsed"
                       ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
            <TextBlock Text="Log Filters" Style="{StaticResource HmiSectionHeaderText}"/>
          </DockPanel>
        </Border>
        <ScrollViewer Grid.Row="1" Margin="8" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="128">
          <WrapPanel>
          <StackPanel Width="180" Margin="0,0,8,8">
            <TextBlock Text="From" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
            <DatePicker x:Name="FromDatePicker" Style="{StaticResource HmiFieldDatePicker}"/>
          </StackPanel>
          <StackPanel Width="180" Margin="0,0,8,8">
            <TextBlock Text="To" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
            <DatePicker x:Name="ToDatePicker" Style="{StaticResource HmiFieldDatePicker}"/>
          </StackPanel>
          <StackPanel Width="170" Margin="0,0,8,8">
            <TextBlock Text="Model / Board" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
            <TextBox x:Name="BoardFilterText" Text=""/>
          </StackPanel>
          <StackPanel Width="150" Margin="0,0,8,8">
            <TextBlock Text="Operator" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
            <TextBox x:Name="OperatorFilterText" Text=""/>
          </StackPanel>
          <StackPanel Width="132" Margin="0,0,8,8">
            <TextBlock Text="Result" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
            <ComboBox x:Name="ResultFilterCombo" SelectedIndex="0">
              <ComboBoxItem Content="ALL" Tag="ALL"/>
              <ComboBoxItem Content="OK" Tag="OK"/>
              <ComboBoxItem Content="REVIEW" Tag="REVIEW"/>
              <ComboBoxItem Content="NG" Tag="NG"/>
            </ComboBox>
          </StackPanel>
          <StackPanel Width="140" Margin="0,0,8,8">
            <TextBlock Text="User Role" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
            <ComboBox x:Name="RoleFilterCombo" SelectedIndex="0">
              <ComboBoxItem Content="ALL" Tag="ALL"/>
              <ComboBoxItem Content="Operator" Tag="Operator"/>
              <ComboBoxItem Content="Engineer" Tag="Engineer"/>
              <ComboBoxItem Content="Admin" Tag="Admin"/>
            </ComboBox>
          </StackPanel>
          <StackPanel Width="270" Margin="0,0,8,8">
            <TextBlock Text="Audit Action Type" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
            <TextBox x:Name="ActionTypeFilterText" Text="" ToolTip="Examples: LOGIN, EXPORT, MAIN_INSPECTION, ENGINE_CONFIG, CALIBRATION"/>
          </StackPanel>
          <WrapPanel Width="282" VerticalAlignment="Bottom" Margin="0,18,8,8">
            <Button Style="{StaticResource ActionBtnGreen}" Content="Apply Filters" Margin="0,0,6,0" Click="OnApplyFiltersClick"/>
            <Button Style="{StaticResource ActionBtn}" Content="Clear" Click="OnClearFiltersClick"/>
          </WrapPanel>
          <TextBlock Width="520"
                     Text="Filter production evidence, operator decisions, exports, and queue activity. Long paths are available through hover tooltips."
                     Style="{StaticResource HmiPageSubtitleText}"
                     VerticalAlignment="Bottom"
                     Margin="0,18,0,8"/>
          </WrapPanel>
        </ScrollViewer>
      </Grid>
    </Border>

    <Border Grid.Row="2" Style="{StaticResource Panel}">
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto" MinHeight="32"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#1A2024" BorderBrush="#343D44" BorderThickness="0,0,0,1" Padding="8,4">
          <DockPanel>
            <TextBlock x:Name="LogSummaryText" DockPanel.Dock="Right" Text="No records loaded" Foreground="#9AA6AF" FontSize="14"/>
            <TextBlock Text="SQLite Logs" Foreground="#DCE5EB" FontWeight="ExtraBold" FontSize="14"/>
          </DockPanel>
        </Border>
        <TabControl Grid.Row="1" Background="#151A1E" BorderBrush="#343D44">
          <TabItem Header="Inspection History">
            <DataGrid x:Name="InspectionGrid" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="1160">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding TimestampLocal}" SortMemberPath="CreatedAtUtc" Width="130" MinWidth="130"/>
                <DataGridTextColumn Header="Board" Binding="{Binding BoardProgram}" Width="120" MinWidth="120"/>
                <DataGridTextColumn Header="Operator" Binding="{Binding OperatorId}" Width="110" MinWidth="110"/>
                <DataGridTextColumn Header="Result" Binding="{Binding Verdict}" Width="90" MinWidth="90"/>
                <DataGridTextColumn Header="Score" Binding="{Binding ScoreDisplay}" SortMemberPath="DifferenceScore" Width="84" MinWidth="84"/>
                <DataGridTextColumn Header="Confidence" Binding="{Binding ConfidenceDisplay}" SortMemberPath="Confidence" Width="110" MinWidth="110"/>
                <DataGridTextColumn Header="Engine" Binding="{Binding InspectionEngine}" Width="170" MinWidth="170"/>
                <DataGridTextColumn Header="Defect" Binding="{Binding SuggestedDefect}" Width="160" MinWidth="160"/>
                <DataGridTextColumn Header="Image" Binding="{Binding ImageName}" Width="280" MinWidth="280"/>
              </DataGrid.Columns>
            </DataGrid>
          </TabItem>
          <TabItem Header="Review / Disposition Events" ToolTip="Operator review and disposition events">
            <DataGrid x:Name="ReviewGrid" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="780">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding TimestampLocal}" SortMemberPath="EventTimeUtc" Width="130" MinWidth="130"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" MinWidth="120"/>
                <DataGridTextColumn Header="Operator" Binding="{Binding OperatorId}" Width="110" MinWidth="110"/>
                <DataGridTextColumn Header="Disposition" Binding="{Binding Disposition}" Width="130" MinWidth="130"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="290" MinWidth="290"/>
              </DataGrid.Columns>
            </DataGrid>
          </TabItem>
          <TabItem Header="Export History">
            <DataGrid x:Name="ExportGrid" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="1140" ToolTip="Export file paths are available through row/cell hover when truncated.">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding TimestampLocal}" SortMemberPath="CreatedAtUtc" Width="130" MinWidth="130"/>
                <DataGridTextColumn Header="Type" Binding="{Binding ExportType}" Width="160" MinWidth="160"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="96" MinWidth="96"/>
                <DataGridTextColumn Header="Verified" Binding="{Binding VerificationStatus}" Width="110" MinWidth="110"/>
                <DataGridTextColumn Header="SHA-256" Binding="{Binding VerificationShaDisplay}" Width="124" MinWidth="124"/>
                <DataGridTextColumn Header="Operator" Binding="{Binding OperatorId}" Width="130" MinWidth="130"/>
                <DataGridTextColumn Header="Audit ID" Binding="{Binding AuditEventDisplay}" Width="100" MinWidth="100"/>
                <DataGridTextColumn Header="Path" Binding="{Binding FilePath}" Width="290" MinWidth="290"/>
              </DataGrid.Columns>
            </DataGrid>
          </TabItem>
          <TabItem Header="Audit Trail">
            <DataGrid x:Name="AuditGrid" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="1160" ToolTip="Traceability means the audit path from user, board, result, export, and upload event.">
              <DataGrid.Columns>
                <DataGridTextColumn Header="UTC Time" Binding="{Binding TimestampUtcDisplay}" SortMemberPath="TimestampUtc" Width="145" MinWidth="145"/>
                <DataGridTextColumn Header="Local Time" Binding="{Binding LocalTimestampDisplay}" Width="145" MinWidth="145"/>
                <DataGridTextColumn Header="User" Binding="{Binding UserId}" Width="120" MinWidth="120"/>
                <DataGridTextColumn Header="Role" Binding="{Binding UserRole}" Width="90" MinWidth="90"/>
                <DataGridTextColumn Header="Station" Binding="{Binding StationId}" Width="120" MinWidth="120"/>
                <DataGridTextColumn Header="Action" Binding="{Binding ActionCategory}" Width="150" MinWidth="150"/>
                <DataGridTextColumn Header="Related ID" Binding="{Binding RelatedEntityDisplay}" Width="150" MinWidth="150"/>
                <DataGridTextColumn Header="Detail" Binding="{Binding ActionDetail}" Width="240" MinWidth="240"/>
              </DataGrid.Columns>
            </DataGrid>
          </TabItem>
          <TabItem Header="MES Upload Queue" ToolTip="MES means factory traceability system. This queue shows results waiting to upload, failed uploads, and sent records.">
            <DockPanel>
              <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Status" Foreground="#8B969E" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="MesQueueStatusFilter" Width="150" SelectionChanged="OnMesQueueStatusFilterChanged" SelectedIndex="0">
                  <ComboBoxItem Content="All" Tag="All"/>
                  <ComboBoxItem Content="Pending" Tag="Pending"/>
                  <ComboBoxItem Content="Failed" Tag="Failed"/>
                  <ComboBoxItem Content="Sent" Tag="Sent"/>
                  <ComboBoxItem Content="Abandoned" Tag="Abandoned"/>
                </ComboBox>
              </StackPanel>
              <DataGrid x:Name="MesSpoolGrid" CanUserSortColumns="True" SelectionMode="Extended" Style="{StaticResource HmiDenseTable}" MinWidth="1120" ToolTip="Endpoint and last-error text may be long; hover the row/cell to inspect the full value.">
                <DataGrid.Columns>
                  <DataGridTextColumn Header="Created" Binding="{Binding CreatedLocal}" SortMemberPath="CreatedAtUtc" Width="130" MinWidth="130"/>
                  <DataGridTextColumn Header="Type" Binding="{Binding PayloadType}" Width="140" MinWidth="140"/>
                  <DataGridTextColumn Header="Lot" Binding="{Binding LotId}" Width="105" MinWidth="105"/>
                  <DataGridTextColumn Header="Board" Binding="{Binding BoardModel}" Width="115" MinWidth="115"/>
                  <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="90" MinWidth="90"/>
                  <DataGridTextColumn Header="Endpoint" Binding="{Binding EndpointUrl}" Width="190" MinWidth="190"/>
                  <DataGridTextColumn Header="Retries" Binding="{Binding RetryDisplay}" Width="90" MinWidth="90"/>
                  <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="96" MinWidth="96"/>
                  <DataGridTextColumn Header="Last Error" Binding="{Binding LastError}" Width="260" MinWidth="260"/>
                </DataGrid.Columns>
              </DataGrid>
            </DockPanel>
          </TabItem>
          <TabItem Header="Central Evidence Sync" ToolTip="Central Sync copies selected evidence to a configured folder or service. It does not approve factory readiness by itself.">
            <DataGrid x:Name="CentralSyncGrid" CanUserSortColumns="True" SelectionMode="Extended" Style="{StaticResource HmiDenseTable}" MinWidth="1080" ToolTip="Endpoint/folder summaries are intentionally compact; hover to inspect the full path or endpoint.">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Created" Binding="{Binding CreatedLocal}" SortMemberPath="CreatedAtUtc" Width="130" MinWidth="130"/>
                <DataGridTextColumn Header="Item Type" Binding="{Binding ItemType}" Width="145" MinWidth="145"/>
                <DataGridTextColumn Header="Item ID" Binding="{Binding ItemId}" Width="100" MinWidth="100"/>
                <DataGridTextColumn Header="Station" Binding="{Binding StationId}" Width="120" MinWidth="120"/>
                <DataGridTextColumn Header="Endpoint" Binding="{Binding EndpointOrFolder}" Width="220" MinWidth="220"/>
                <DataGridTextColumn Header="Retries" Binding="{Binding RetryDisplay}" Width="90" MinWidth="90"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="96" MinWidth="96"/>
                <DataGridTextColumn Header="Last Error" Binding="{Binding LastError}" Width="240" MinWidth="240"/>
              </DataGrid.Columns>
            </DataGrid>
          </TabItem>
          <TabItem Header="Pilot Issues">
            <Grid Margin="8">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <WrapPanel Margin="0,0,0,8">
                <TextBlock Text="Category" Foreground="#9AA6AF" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="PilotIssueCategoryFilterCombo" Width="145" SelectedIndex="0" Margin="0,0,8,0" SelectionChanged="OnPilotIssueFilterChanged"/>
                <TextBlock Text="Status" Foreground="#9AA6AF" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="PilotIssueStatusFilterCombo" Width="125" SelectedIndex="0" Margin="0,0,8,0" SelectionChanged="OnPilotIssueFilterChanged"/>
                <TextBlock Text="Severity" Foreground="#9AA6AF" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="PilotIssueSeverityFilterCombo" Width="115" SelectedIndex="0" Margin="0,0,12,0" SelectionChanged="OnPilotIssueFilterChanged">
                  <ComboBoxItem Content="All" Tag="All"/>
                  <ComboBoxItem Content="Critical" Tag="Critical"/>
                  <ComboBoxItem Content="High" Tag="High"/>
                  <ComboBoxItem Content="Medium" Tag="Medium"/>
                  <ComboBoxItem Content="Low" Tag="Low"/>
                </ComboBox>
                <Button Style="{StaticResource ActionBtnGreen}" Content="Good Board Marked Defect" Margin="0,0,6,0" Click="OnCreateFalseCallIssueClick" ToolTip="False Call: the system marked a good board as a defect."/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Possible Missed Defect" Margin="0,0,6,0" Click="OnCreatePossibleEscapeIssueClick" ToolTip="Possible Escape: the system may have missed a real defect."/>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Refresh Issues" Margin="0,0,6,0" Click="OnRefreshPilotIssuesClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Close Selected" Margin="0,0,6,0" Click="OnClosePilotIssueClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Export Issue Report" Click="OnExportPilotIssueReportClick"/>
                <TextBlock x:Name="PilotIssueSummaryText" Text="Pilot issues not loaded." Foreground="#DCE5EB" VerticalAlignment="Center" Margin="12,0,0,0"/>
              </WrapPanel>
              <DataGrid x:Name="PilotIssuesGrid" Grid.Row="1" CanUserSortColumns="True" SelectionMode="Single" Style="{StaticResource HmiDenseTable}" MinWidth="1900" ToolTip="Use this list to close manual test findings before a client demo. False Call means a good board was marked defect. Possible Escape means a real defect may have been missed.">
                <DataGrid.Columns>
                  <DataGridTextColumn Header="ID" Binding="{Binding IssueId}" Width="140" MinWidth="140"/>
                  <DataGridTextColumn Header="Created" Binding="{Binding CreatedLocal}" SortMemberPath="CreatedAtUtc" Width="130" MinWidth="130"/>
                  <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="125" MinWidth="125"/>
                  <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Page" Binding="{Binding PageName}" Width="140" MinWidth="140"/>
                  <DataGridTextColumn Header="Steps" Binding="{Binding ReproductionSteps}" Width="210" MinWidth="210"/>
                  <DataGridTextColumn Header="Expected" Binding="{Binding ExpectedBehavior}" Width="190" MinWidth="190"/>
                  <DataGridTextColumn Header="Actual" Binding="{Binding ActualBehavior}" Width="190" MinWidth="190"/>
                  <DataGridTextColumn Header="Evidence" Binding="{Binding ScreenshotPath}" Width="200" MinWidth="200"/>
                  <DataGridTextColumn Header="Board" Binding="{Binding BoardModel}" Width="115" MinWidth="115"/>
                  <DataGridTextColumn Header="Lot" Binding="{Binding LotId}" Width="100" MinWidth="100"/>
                  <DataGridTextColumn Header="Inspection" Binding="{Binding RelatedInspectionId}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" Width="120" MinWidth="120"/>
                  <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="220" MinWidth="220"/>
                </DataGrid.Columns>
              </DataGrid>
            </Grid>
          </TabItem>
          <TabItem Header="UI Stability Test" ToolTip="Cycles through major screens to catch slow navigation, memory growth, and recoverable crashes.">
            <Grid Margin="8">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <WrapPanel Margin="0,0,0,8">
                <TextBlock Text="Profile" Foreground="#9AA6AF" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="UiStabilityProfileCombo" Width="235" SelectedIndex="1" Margin="0,0,8,0">
                  <ComboBoxItem Content="5-minute smoke" Tag="{x:Static services:UiNavigationSoakProfile.FiveMinuteSmoke}"/>
                  <ComboBoxItem Content="30-minute local stability" Tag="{x:Static services:UiNavigationSoakProfile.ThirtyMinuteLocalStability}"/>
                  <ComboBoxItem Content="4-hour client demo readiness" Tag="{x:Static services:UiNavigationSoakProfile.FourHourClientDemoReadiness}"/>
                  <ComboBoxItem Content="8-hour factory PoC" Tag="{x:Static services:UiNavigationSoakProfile.EightHourFactoryPoC}"/>
                </ComboBox>
                <CheckBox x:Name="UiStabilityHeavyLoadsCheck" Content="Exercise heavy page loads" Foreground="#DCE5EB" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Run UI Stability Test" Margin="0,0,6,0" Click="OnRunUiStabilityTestClick"/>
              </WrapPanel>
              <TextBlock Grid.Row="1" x:Name="UiStabilityStatsText"
                         Text="UI stability not run. Use 30-minute or better PASS evidence before client demo export."
                         Foreground="#DCE5EB" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,8"/>
              <DataGrid Grid.Row="2" x:Name="UiStabilityEventsGrid" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="980">
                <DataGrid.Columns>
                  <DataGridTextColumn Header="UTC" Binding="{Binding TimestampUtc}" Width="150" MinWidth="150"/>
                  <DataGridTextColumn Header="Cycle" Binding="{Binding Cycle}" Width="80" MinWidth="80"/>
                  <DataGridTextColumn Header="Page" Binding="{Binding PageName}" Width="180" MinWidth="180"/>
                  <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="96" MinWidth="96"/>
                  <DataGridTextColumn Header="Load ms" Binding="{Binding PageLoadMilliseconds}" Width="100" MinWidth="100"/>
                  <DataGridTextColumn Header="Heartbeat" Binding="{Binding HeartbeatMilliseconds}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Memory MB" Binding="{Binding WorkingSetMegabytes}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="240" MinWidth="240"/>
                </DataGrid.Columns>
              </DataGrid>
            </Grid>
          </TabItem>
          <TabItem Header="Management Dashboard" ToolTip="Engineer/Admin view for trend review. Operators use Main Inspection for normal work.">
            <Grid Margin="8">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="120"/>
                  <ColumnDefinition Width="8"/>
                  <ColumnDefinition Width="130"/>
                  <ColumnDefinition Width="8"/>
                  <ColumnDefinition Width="165"/>
                  <ColumnDefinition Width="8"/>
                  <ColumnDefinition Width="190"/>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                  <TextBlock Text="Lot" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
                  <TextBox x:Name="ManagementLotFilterText"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                  <TextBlock Text="Model Version" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
                  <TextBox x:Name="ManagementModelFilterText"/>
                </StackPanel>
                <StackPanel Grid.Column="4">
                  <TextBlock Text="Board / Operator" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
                  <TextBlock Text="Uses top filters" Foreground="#DCE5EB" FontSize="14" VerticalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="6">
                  <TextBlock Text="Deployment Profile" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,4"/>
                  <ComboBox x:Name="ManagementDeploymentProfileCombo" SelectedIndex="0" Style="{StaticResource FilterCombo}"/>
                </StackPanel>
                <WrapPanel Grid.Column="8" VerticalAlignment="Bottom">
                  <Button Style="{StaticResource ActionBtnGreen}" Content="Refresh Dashboard" Margin="0,0,6,0" Click="OnRefreshManagementDashboardClick"/>
                  <Button Style="{StaticResource ActionBtnAmber}" Content="Export Dashboard HTML/CSV/PDF" Click="OnExportManagementDashboardClick"/>
                </WrapPanel>
              </Grid>
              <TextBlock Grid.Row="1" x:Name="ManagementDashboardSummaryText" Text="Dashboard not loaded." Foreground="#DCE5EB" FontSize="14" TextWrapping="Wrap" Margin="0,8,0,8"/>
              <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*"/>
                  <ColumnDefinition Width="8"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                  <RowDefinition Height="*"/>
                  <RowDefinition Height="8"/>
                  <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DataGrid x:Name="ManagementDefectGrid" Grid.Row="0" Grid.Column="0" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="560" ToolTip="False calls are OK boards flagged for review or NG. Escapes are known NG boards that may be missed.">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Defect Class" Binding="{Binding Name}" Width="170" MinWidth="170"/>
                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="86" MinWidth="86"/>
                    <DataGridTextColumn Header="False Calls" Binding="{Binding FalseCalls}" Width="112" MinWidth="112"/>
                    <DataGridTextColumn Header="Escapes" Binding="{Binding PossibleEscapes}" Width="96" MinWidth="96"/>
                    <DataGridTextColumn Header="Reviews" Binding="{Binding Reviews}" Width="96" MinWidth="96"/>
                  </DataGrid.Columns>
                </DataGrid>
                <DataGrid x:Name="ManagementRoiGrid" Grid.Row="0" Grid.Column="2" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="560" ToolTip="ROI means region of interest: the board image area checked for a defect.">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="ROI / RefDes" Binding="{Binding Name}" Width="170" MinWidth="170"/>
                    <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="86" MinWidth="86"/>
                    <DataGridTextColumn Header="False Calls" Binding="{Binding FalseCalls}" Width="112" MinWidth="112"/>
                    <DataGridTextColumn Header="Escapes" Binding="{Binding PossibleEscapes}" Width="96" MinWidth="96"/>
                    <DataGridTextColumn Header="Reviews" Binding="{Binding Reviews}" Width="96" MinWidth="96"/>
                  </DataGrid.Columns>
                </DataGrid>
                <DataGrid x:Name="ManagementModelTrendGrid" Grid.Row="2" Grid.Column="0" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="590">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Model" Binding="{Binding ModelVersion}" Width="170" MinWidth="170"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="80" MinWidth="80"/>
                    <DataGridTextColumn Header="OK" Binding="{Binding Ok}" Width="72" MinWidth="72"/>
                    <DataGridTextColumn Header="NG" Binding="{Binding Ng}" Width="72" MinWidth="72"/>
                    <DataGridTextColumn Header="Review" Binding="{Binding Review}" Width="90" MinWidth="90"/>
                    <DataGridTextColumn Header="Avg ms" Binding="{Binding AverageInspectionTimeMs}" Width="90" MinWidth="90"/>
                  </DataGrid.Columns>
                </DataGrid>
                <DataGrid x:Name="ManagementLotModelGrid" Grid.Row="2" Grid.Column="2" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="780" ToolTip="False-call rate and possible escapes are review signals, not automatic production release decisions.">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Lot" Binding="{Binding LotId}" Width="100" MinWidth="100"/>
                    <DataGridTextColumn Header="Board" Binding="{Binding BoardModel}" Width="115" MinWidth="115"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding ModelVersion}" Width="160" MinWidth="160"/>
                    <DataGridTextColumn Header="Total" Binding="{Binding Total}" Width="80" MinWidth="80"/>
                    <DataGridTextColumn Header="False Calls" Binding="{Binding FalseCalls}" Width="112" MinWidth="112"/>
                    <DataGridTextColumn Header="Escapes" Binding="{Binding PossibleEscapes}" Width="96" MinWidth="96"/>
                    <DataGridTextColumn Header="False-call Rate" Binding="{Binding FalseCallRate}" Width="120" MinWidth="120"/>
                  </DataGrid.Columns>
                </DataGrid>
              </Grid>
            </Grid>
          </TabItem>
          <TabItem Header="Factory Readiness" ToolTip="Shows what evidence is present, what is missing, and why it matters. Acceptance rows are evidence checks, not final certification.">
            <DataGrid x:Name="FactoryReadinessGrid" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="900">
              <DataGrid.Columns>
                <DataGridTextColumn Header="Category" Binding="{Binding Name}" Width="190" MinWidth="190"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" MinWidth="110"/>
                <DataGridTextColumn Header="Evidence" Binding="{Binding Evidence}" Width="330" MinWidth="330"/>
                <DataGridTextColumn Header="Next Action" Binding="{Binding NextAction}" Width="270" MinWidth="270"/>
              </DataGrid.Columns>
            </DataGrid>
          </TabItem>
          <TabItem Header="Stage 1 Readiness" ToolTip="Independent Stage 1 customer-validation readiness. This does not require or claim real hardware readiness.">
            <Grid Margin="8">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <DockPanel Margin="0,0,0,8">
                <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
                  <Button Style="{StaticResource ActionBtnBlue}" Content="Refresh Stage 1 Readiness" Margin="0,0,6,6" Click="OnRefreshStage1ReadinessClick"/>
                  <Button Style="{StaticResource ActionBtnAmber}" Content="Export Stage 1 Readiness Report" Margin="0,0,6,6" Click="OnExportStage1ReadinessReportClick"/>
                  <Button Style="{StaticResource ActionBtn}" Content="Open Sample Dataset Guide" Margin="0,0,6,6" Click="OnOpenSampleDatasetGuideClick"/>
                  <Button Style="{StaticResource ActionBtn}" Content="Open Latest Validation Package" Margin="0,0,6,6" Click="OnOpenLatestValidationPackageClick"/>
                  <Button Style="{StaticResource ActionBtn}" Content="Open Latest Benchmark Report" Margin="0,0,0,6" Click="OnOpenLatestBenchmarkReportClick"/>
                </WrapPanel>
                <StackPanel Margin="0,0,12,0">
                  <TextBlock Text="Stage 1 Readiness" Style="{StaticResource HmiSectionHeaderText}"/>
                  <TextBlock x:Name="Stage1ReadinessSummaryText"
                             Text="Stage 1 readiness not evaluated."
                             Foreground="#DCE5EB"
                             FontSize="14"
                             TextWrapping="Wrap"/>
                  <TextBlock Text="Stage 1 PASS is scoped to local image-validation evidence. Real camera, lighting, robot, PLC safety, production MES, ERP, and full factory automation remain not validated unless their own evidence exists."
                             Style="{StaticResource HmiTechnicalHintText}"
                             TextWrapping="Wrap"/>
                </StackPanel>
              </DockPanel>
              <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,8">
                <Border x:Name="Stage1ReadinessStatusCard" Style="{StaticResource FieldBox}" Margin="0,0,6,0">
                  <StackPanel>
                    <TextBlock Text="Status" Foreground="#9AA6AF" FontSize="14"/>
                    <TextBlock x:Name="Stage1ReadinessStatusText" Text="NOT RUN" FontWeight="Bold" FontSize="18" TextWrapping="Wrap"/>
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource FieldBox}" Margin="0,0,6,0">
                  <StackPanel>
                    <TextBlock Text="Preflight" Foreground="#9AA6AF" FontSize="14"/>
                    <TextBlock x:Name="Stage1PreflightText" Text="--" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource FieldBox}" Margin="0,0,6,0">
                  <StackPanel>
                    <TextBlock Text="Batch Run" Foreground="#9AA6AF" FontSize="14"/>
                    <TextBlock x:Name="Stage1BatchText" Text="--" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource FieldBox}" Margin="0,0,6,0">
                  <StackPanel>
                    <TextBlock Text="Benchmark" Foreground="#9AA6AF" FontSize="14"/>
                    <TextBlock x:Name="Stage1BenchmarkText" Text="--" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                  </StackPanel>
                </Border>
                <Border Style="{StaticResource FieldBox}">
                  <StackPanel>
                    <TextBlock Text="Validation Package" Foreground="#9AA6AF" FontSize="14"/>
                    <TextBlock x:Name="Stage1PackageText" Text="--" FontWeight="Bold" FontSize="14" TextWrapping="Wrap" ToolTip="{Binding RelativeSource={RelativeSource Self}, Path=Text}"/>
                  </StackPanel>
                </Border>
              </UniformGrid>
              <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="2*"/>
                  <ColumnDefinition Width="8"/>
                  <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <DataGrid x:Name="Stage1ReadinessGrid" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="900" ToolTip="Stage 1 readiness checks are independent from full factory/hardware readiness.">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="Check" Binding="{Binding Name}" Width="220" MinWidth="220"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" MinWidth="120"/>
                    <DataGridTextColumn Header="Evidence" Binding="{Binding Evidence}" Width="360" MinWidth="360"/>
                    <DataGridTextColumn Header="Next Action" Binding="{Binding NextAction}" Width="300" MinWidth="300"/>
                  </DataGrid.Columns>
                </DataGrid>
                <Border Grid.Column="2" Style="{StaticResource FieldBox}">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="*"/>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Missing Evidence" Style="{StaticResource HmiSectionHeaderText}" Margin="0,0,0,6"/>
                    <ListBox x:Name="Stage1MissingEvidenceList"
                             Grid.Row="1"
                             MinHeight="120"
                             Background="#151A1E"
                             BorderBrush="#343D44"
                             Foreground="#FFE1A3"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                    <TextBlock Grid.Row="2" Text="Next Recommended Action" Style="{StaticResource HmiSectionHeaderText}" Margin="0,10,0,6"/>
                    <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                      <TextBlock x:Name="Stage1NextActionText" Text="Run the Stage 1 validation workflow." Foreground="#DCE5EB" FontSize="14" TextWrapping="Wrap"/>
                    </ScrollViewer>
                  </Grid>
                </Border>
              </Grid>
            </Grid>
          </TabItem>
          <TabItem Header="Standards &amp; Quality Checklist" ToolTip="Maps project HMI/software-quality expectations to concrete evidence. This is standards-aligned evidence, not certification.">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <DockPanel Margin="8">
                <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
                  <Button Style="{StaticResource ActionBtnBlue}" Content="Refresh Checklist" Margin="0,0,6,0" Click="OnRefreshStandardsTraceabilityClick"/>
                  <Button Style="{StaticResource ActionBtnAmber}" Content="Export HTML/PDF/JSON" Click="OnExportStandardsTraceabilityClick"/>
                </WrapPanel>
                <TextBlock x:Name="StandardsTraceabilitySummaryText"
                           Text="Standards traceability not loaded."
                           Foreground="#DCE5EB"
                           FontSize="14"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0"/>
              </DockPanel>
              <DataGrid x:Name="StandardsTraceabilityGrid" Grid.Row="1" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="1500" ToolTip="Long evidence paths and notes are available through hover when truncated.">
                <DataGrid.Columns>
                  <DataGridTextColumn Header="Source" Binding="{Binding StandardOrSource}" Width="180" MinWidth="180"/>
                  <DataGridTextColumn Header="Requirement" Binding="{Binding PrincipleOrRequirement}" Width="300" MinWidth="300"/>
                  <DataGridTextColumn Header="ID" Binding="{Binding ProjectRequirementId}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Evidence Type" Binding="{Binding EvidenceType}" Width="170" MinWidth="170"/>
                  <DataGridTextColumn Header="Evidence Path" Binding="{Binding EvidencePath}" Width="280" MinWidth="280"/>
                  <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" MinWidth="120"/>
                  <DataGridTextColumn Header="Blocking" Binding="{Binding BlockingLevel}" Width="130" MinWidth="130"/>
                  <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="310" MinWidth="310"/>
                </DataGrid.Columns>
              </DataGrid>
            </Grid>
          </TabItem>
          <TabItem Header="Evidence Completion" ToolTip="Shows missing evidence by stage. Completion is not the same as production release approval.">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <StackPanel Orientation="Horizontal" Margin="8">
                <Button Style="{StaticResource ActionBtnBlue}" Content="Refresh Evidence List" Click="OnRefreshCompletionMatrixClick"/>
                <TextBlock x:Name="CompletionMatrixSummaryText" Text="Evidence list not loaded." Foreground="#DCE5EB" FontSize="14" VerticalAlignment="Center" TextWrapping="Wrap" Margin="12,0,0,0"/>
              </StackPanel>
              <DataGrid x:Name="CompletionMatrixGrid" Grid.Row="1" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="1360">
                <DataGrid.Columns>
                  <DataGridTextColumn Header="Stage" Binding="{Binding Stage}" Width="220" MinWidth="220"/>
                  <DataGridTextColumn Header="Complete" Binding="{Binding PercentDisplay}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Evidence Score" Binding="{Binding EvidenceSummary}" Width="310" MinWidth="310"/>
                  <DataGridTextColumn Header="Missing" Binding="{Binding MissingEvidenceDisplay}" Width="360" MinWidth="360"/>
                  <DataGridTextColumn Header="Next Action" Binding="{Binding NextActionsDisplay}" Width="360" MinWidth="360"/>
                </DataGrid.Columns>
              </DataGrid>
            </Grid>
          </TabItem>
          <TabItem Header="Factory Acceptance" ToolTip="Factory acceptance checklist for evidence collection and signoff. Do not overclaim production readiness from a single checklist row.">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
              </Grid.RowDefinitions>
              <StackPanel Orientation="Horizontal" Margin="8">
                <TextBlock Text="Deployment Profile" Foreground="#9AA6AF" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="FactoryAcceptanceProfileCombo" Width="230" SelectedIndex="0" Style="{StaticResource FilterCombo}"/>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Generate Checklist" Margin="8,0,0,0" Click="OnGenerateFactoryAcceptanceChecklistClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Export FAT Package" Margin="8,0,0,0" Click="OnExportFactoryAcceptanceChecklistClick"/>
              </StackPanel>
              <DataGrid x:Name="FactoryAcceptanceGrid" Grid.Row="1" CanUserSortColumns="True" Style="{StaticResource HmiDenseTable}" MinWidth="1480" ToolTip="Evidence paths and notes may be long; hover cells to inspect full values.">
                <DataGrid.Columns>
                  <DataGridTextColumn Header="ID" Binding="{Binding RequirementId}" Width="110" MinWidth="110"/>
                  <DataGridTextColumn Header="Requirement" Binding="{Binding RequirementText}" Width="260" MinWidth="260"/>
                  <DataGridTextColumn Header="Required Evidence" Binding="{Binding RequiredEvidence}" Width="270" MinWidth="270"/>
                  <DataGridTextColumn Header="Evidence Path" Binding="{Binding CurrentEvidencePath}" Width="260" MinWidth="260"/>
                  <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="120" MinWidth="120"/>
                  <DataGridTextColumn Header="Signoff Role" Binding="{Binding SignoffRole}" Width="130" MinWidth="130"/>
                  <DataGridTextColumn Header="Signed By" Binding="{Binding SignedBy}" Width="120" MinWidth="120"/>
                  <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="310" MinWidth="310"/>
                </DataGrid.Columns>
              </DataGrid>
            </Grid>
          </TabItem>
        </TabControl>
      </Grid>
    </Border>

    <Border Grid.Row="4" Style="{StaticResource Panel}">
      <Grid Margin="8">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="420"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock Text="Auto-Archive Policy" Foreground="#9AA6AF" FontSize="14" Margin="0,0,0,3"/>
          <TextBlock x:Name="ArchivePolicyText"
                     Text="Logs older than 30 days are copied into LogArchive during startup. Source rows remain in place, so the archive is reversible and audit-safe."
                     TextWrapping="Wrap" FontSize="14" Foreground="#DCE5EB"/>
          <TextBlock x:Name="StatusText" Text="Ready." Foreground="#9AA6AF" FontSize="14" Margin="0,6,0,0"/>
          <TextBlock x:Name="PackagePathText" Text="Latest customer package: --" Foreground="#DCE5EB" FontSize="14" Margin="0,3,0,0" Style="{StaticResource FactoryTrimmedTextWithTooltip}"/>
          <TextBlock x:Name="SoakReportPathText" Text="Latest soak-test report: --" Foreground="#DCE5EB" FontSize="14" Margin="0,3,0,0" Style="{StaticResource FactoryTrimmedTextWithTooltip}"/>
          <TextBlock x:Name="BuildEvidenceSummaryText" Text="Build/test evidence: --" Foreground="#DCE5EB" FontSize="14" Margin="0,3,0,0" Style="{StaticResource FactoryTrimmedTextWithTooltip}"/>
          <TextBlock x:Name="ClientDemoGateText" Text="Client demo readiness: not evaluated" Foreground="#E1A334" FontSize="14" Margin="0,3,0,0" TextWrapping="Wrap"/>
          <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
            <ProgressBar x:Name="WorkProgressBar" Width="190" Height="12" Minimum="0" Maximum="100" Value="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
            <Button x:Name="CancelWorkButton" Style="{StaticResource MiniBtn}" Content="Cancel" IsEnabled="False" Click="OnCancelWorkClick"/>
          </StackPanel>
        </StackPanel>
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="250">
        <StackPanel>
          <Border Style="{StaticResource FieldBox}" Margin="0,0,0,8">
            <StackPanel>
              <TextBlock Text="Evidence Exports" Style="{StaticResource HmiSectionHeaderText}" Margin="0,0,0,6"/>
              <WrapPanel>
                <Button Style="{StaticResource ActionBtnGreen}" Content="Inspection History CSV" Margin="0,0,6,6" Click="OnExportInspectionHistoryClick"/>
                <Button Style="{StaticResource ActionBtnGreen}" Content="Review Log CSV" Margin="0,0,6,6" Click="OnExportReviewLogClick"/>
                <Button Style="{StaticResource ActionBtnGreen}" Content="Audit Trail CSV" Margin="0,0,6,6" Click="OnExportAuditTrailClick"/>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Annotated Overlays" Margin="0,0,6,6" Click="OnExportAnnotatedOverlaysClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Verify Selected Export" Margin="0,0,6,6" Click="OnVerifySelectedExportClick"/>
              </WrapPanel>
            </StackPanel>
          </Border>
          <Border Style="{StaticResource FieldBox}" Margin="0,0,0,8">
            <StackPanel>
              <TextBlock Text="Readiness and Quality Gates" Style="{StaticResource HmiSectionHeaderText}" Margin="0,0,0,6"/>
              <WrapPanel>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Create Stage 1 Customer Package" Margin="0,0,6,6" Click="OnExportCustomerPackageClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Export Readiness Evidence" Margin="0,0,6,6" Click="OnExportFactoryReadinessPackageClick" ToolTip="Exports the readiness evidence package. This is not final production certification."/>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Client Demo Readiness" Margin="0,0,6,6" Click="OnClientDemoReadinessClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Export Demo Gate" Margin="0,0,6,6" Click="OnExportClientDemoReadinessClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Export Standards Matrix" Margin="0,0,6,6" Click="OnExportStandardsTraceabilityClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Export FAT Checklist" Margin="0,0,6,6" Click="OnExportFactoryAcceptanceChecklistClick"/>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Import Build/Test Evidence" Margin="0,0,6,6" Click="OnImportBuildTestEvidenceClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Open Build Evidence Folder" Margin="0,0,6,6" Click="OnOpenBuildEvidenceFolderClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Soak Test" Margin="0,0,6,6" Click="OnRunSoakTestClick"/>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Performance Benchmark" Margin="0,0,6,6" Click="OnRunPerformanceBenchmarkClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Export Latest Soak Evidence" Margin="0,0,6,6" Click="OnExportLatestSoakEvidenceClick"/>
              </WrapPanel>
            </StackPanel>
          </Border>
          <Border Style="{StaticResource FieldBox}" Margin="0,0,0,8">
            <StackPanel>
              <TextBlock Text="Database and Image Index" Style="{StaticResource HmiSectionHeaderText}" Margin="0,0,0,6"/>
              <WrapPanel>
                <Button Style="{StaticResource ActionBtn}" Content="DB Integrity" Margin="0,0,6,6" Click="OnRunDbIntegrityCheckClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Database Health" Margin="0,0,6,6" Click="OnOpenDatabaseHealthClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Verify Paths" Margin="0,0,6,6" Click="OnVerifyImagePathsClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Rebuild Image Index" Margin="0,0,6,6" Click="OnRebuildImageIndexClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Lock Recipe" Margin="0,0,6,6" Click="OnLockActiveRecipeClick"/>
              </WrapPanel>
            </StackPanel>
          </Border>
          <Border Style="{StaticResource FieldBox}" Margin="0,0,0,8">
            <StackPanel>
              <TextBlock Text="MES Traceability Queue" Style="{StaticResource HmiSectionHeaderText}" Margin="0,0,0,6"/>
              <WrapPanel>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Send Result to Traceability System" Margin="0,0,6,6" Click="OnUploadMesMockClick" ToolTip="MES means factory traceability system. Mock mode creates local evidence only."/>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Run Traceability Test" Margin="0,0,6,6" Click="OnRunTraceabilityTestClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Retry Selected Upload" Margin="0,0,6,6" Click="OnRetrySelectedMesSpoolClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Retry All Waiting Uploads" Margin="0,0,6,6" Click="OnRetryMesSpoolClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Export Queue Report" Margin="0,0,6,6" Click="OnExportMesQueueReportClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Export MES Contract" Margin="0,0,6,6" Click="OnExportMesContractClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Mark MES Abandoned" Margin="0,0,6,6" Click="OnAbandonMesSpoolClick"/>
              </WrapPanel>
            </StackPanel>
          </Border>
          <Border Style="{StaticResource FieldBox}">
            <StackPanel>
              <TextBlock Text="Central Evidence Sync" Style="{StaticResource HmiSectionHeaderText}" Margin="0,0,0,6"/>
              <WrapPanel>
                <Button Style="{StaticResource ActionBtnBlue}" Content="Queue Central Sync" Margin="0,0,6,6" Click="OnQueueCentralSyncClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Retry Selected Central" Margin="0,0,6,6" Click="OnRetrySelectedCentralSyncClick"/>
                <Button Style="{StaticResource ActionBtnAmber}" Content="Retry All Central" Margin="0,0,6,6" Click="OnRetryCentralSyncClick"/>
                <Button Style="{StaticResource ActionBtn}" Content="Export Central Sync Report" Margin="0,0,6,6" Click="OnExportCentralSyncReportClick"/>
              </WrapPanel>
            </StackPanel>
          </Border>
        </StackPanel>
        </ScrollViewer>
      </Grid>
    </Border>
  </Grid>
</UserControl>

